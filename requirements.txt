# ── WSGI server ───────────────────────────────────────────────────────────────
gunicorn>=21.2.0

# ── Web framework ─────────────────────────────────────────────────────────────
Flask>=3.1.0
Flask-Limiter>=3.5.0
Authlib>=1.3.0
requests>=2.32.0
python-dotenv>=1.0.0
markupsafe>=2.1.0

# ── Cloudflare R2 (media bucket + per-user SQLite DB bucket) ──────────────────
boto3>=1.34.0

# ── On-chain signing — EVM (BSC / Avalanche) ──────────────────────────────────
eth-account>=0.10.0

# ── On-chain signing — Aptos ──────────────────────────────────────────────────
aptos-sdk>=0.8.0

# NOTE: psycopg2 / SQLAlchemy removed — using built-in sqlite3 module.

# ── Transactional email (Brevo / Sendinblue) ─────────────────────────────────
brevo-python>=3.1.0

# ── Web Push / PWA (push notifications) ──────────────────────────────────────
pywebpush>=2.0.0

# ── Response compression ─────────────────────────────────────────────────────
flask-compress>=1.15

# ── HTML parsing for Open Graph link previews ─────────────────────────────────
beautifulsoup4>=4.12.0
