# ── Web framework ─────────────────────────────────────────────────────────────
Flask>=3.1.0
Flask-Limiter>=3.5.0
Authlib>=1.3.0
requests>=2.32.0
python-dotenv>=1.0.0

# ── Cloudflare R2 object storage (S3-compatible via boto3) ───────────────────
boto3>=1.34.0

# ── PostgreSQL driver (replaces SQLite) ───────────────────────────────────────
# Use psycopg2-binary for quick deploys (Render, Heroku, Railway).
# Switch to psycopg2 (without -binary) for production on bare metal.
psycopg2-binary>=2.9.9

# ── On-chain signing — EVM (BSC / Avalanche) ──────────────────────────────────
eth-account>=0.10.0

# ── On-chain signing — Aptos ──────────────────────────────────────────────────
aptos-sdk>=0.8.0
